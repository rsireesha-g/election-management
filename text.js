// Create a Node.js application using Express as middleware. --done
// You are provided with 4 database tables: Voters, Candidates, Votes, Elections.(Election task) --done
// Design models/schema for each of the 4 tables. --done
// Create separate route files for each table. --done
// Connect the application to a database ( MySQL, etc.). --done
// Use environment variables for sensitive config (via .env file). --done


// Implement full CRUD operations (Create, Read--done, Update, Delete) for each table.
// Use controller functions to handle business logic (recommended).
// Test all routes using Postman to ensure CRUD operations work correctly.
// Follow a proper modular folder structure
// Handle errors and edge cases with appropriate status codes.
// Write clean, readable, and well-commented code. 
